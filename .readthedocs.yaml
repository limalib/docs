version: "2"
sphinx:
  # Path to your Sphinx configuration file.
  configuration: conf.py

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt
